---

layout: post
title: "Shields.io 动态徽章完全教程"
date: 2026-01-24 14:39:12 +0800
categories: [技术教程]
tags: [shields.io, 徽章, GitHub, 动态徽章, 保姆级教程, 自动化, 开源,教程]

---

> 前言：Shields.io 是一个免费的开源徽章生成服务，每月处理超过 7.7 亿次请求。它能生成美观、风格统一的 SVG 徽章，用于展示项目状态、版本号、下载量、统计数据等信息。无论你是做开源项目、个人作品集，还是团队文档，动态徽章都能让信息展示更专业、更实时。

本教程特点：
- 从零开始：不需要任何前置知识
- 保姆级步骤：每个操作都有详细说明和代码
- 全覆盖：从静态徽章到高级动态徽章，从手动制作到自动化部署
- 避坑指南：包含常见错误和解决方案

---

# 第一章：静态徽章 - 5分钟上手

## 1.1 最基础的静态徽章

静态徽章是固定内容的徽章，URL 格式极其简单：

```
https://img.shields.io/badge/左边标签-右边内容-颜色
```

示例：

```
https://img.shields.io/badge/版本-1.0.0-blue
```

在 Markdown 中使用：

```markdown
![版本](https://img.shields.io/badge/版本-1.0.0-blue)
```

三个必填部分：
1. 左边标签（LABEL）：徽章左侧的文字
2. 右边内容（MESSAGE）：徽章右侧的文字
3. 颜色（COLOR）：右侧背景颜色

## 1.2 字符编码问题（重要！）

如果你的文字包含特殊字符，必须进行 URL 编码，否则徽章会失效：

字符	编码后	示例	
空格	`%20`	`Hello%20World`	
`-` 连字符	`--`	`My--Project`	
`_` 下划线	`__`	`My__Project`	
`%`	`%25`	`100%25`	
`/`	`%2F`	`v1%2F2`	

在线编码工具：搜索"URL 编码工具"即可找到

1.3 推荐格式（更安全）

对于复杂内容，使用 `static/v1` 格式更可靠：

```
https://img.shields.io/static/v1?label=左边标签&message=右边内容&color=颜色
```

示例：

```markdown
![状态](https://img.shields.io/static/v1?label=构建状态&message=通过&color=success)
```

优点：
- 自动处理特殊字符
- 参数清晰易读
- 支持更多自定义选项

---

# 第二章：GitHub 内置动态徽章（最实用）

## 2.1 为什么从 GitHub 开始

如果你的代码托管在 GitHub，这是最简单、最稳定的动态徽章方案。无需配置，直接调用官方 API。

## 2.2 常用 GitHub 徽章大全

徽章类型	URL 格式	实际示例	
Stars 数	`/github/stars/{用户}/{仓库}`	`https://img.shields.io/github/stars/microsoft/vscode`	
Forks 数	`/github/forks/{用户}/{仓库}`	`https://img.shields.io/github/forks/microsoft/vscode`	
Issues	`/github/issues/{用户}/{仓库}`	`https://img.shields.io/github/issues/microsoft/vscode`	
最后提交	`/github/last-commit/{用户}/{仓库}`	`https://img.shields.io/github/last-commit/microsoft/vscode`	
发布版本	`/github/release/{用户}/{仓库}`	`https://img.shields.io/github/release/microsoft/vscode`	
代码大小	`/github/repo-size/{用户}/{仓库}`	`https://img.shields.io/github/repo-size/microsoft/vscode`	
语言分布	`/github/languages/top/{用户}/{仓库}`	`https://img.shields.io/github/languages/top/microsoft/vscode`	
工作流状态	`/github/workflow/status/{用户}/{仓库}/{工作流名}`	`https://img.shields.io/github/workflow/status/用户/仓库/CI`	
个人粉丝	`/github/followers/{用户名}`	`https://img.shields.io/github/followers/your-username`	

## 2.3 实战：为项目添加完整徽章组

复制以下代码到你的项目 `README.md`：

```markdown
<!-- 将 your-username 和 your-repo 替换为你的实际信息 -->

<!-- 第一行：项目核心状态 -->
![GitHub Stars](https://img.shields.io/github/stars/your-username/your-repo?style=flat-square&logo=github&color=gold)
![GitHub Forks](https://img.shields.io/github/forks/your-username/your-repo?style=flat-square&logo=github)
![GitHub Issues](https://img.shields.io/github/issues/your-username/your-repo?style=flat-square&logo=github)

<!-- 第二行：代码质量 -->
![GitHub last commit](https://img.shields.io/github/last-commit/your-username/your-repo?style=flat-square&logo=git&color=success)
![GitHub code size](https://img.shields.io/github/languages/code-size/your-username/your-repo?style=flat-square&logo=code)
![GitHub license](https://img.shields.io/github/license/your-username/your-repo?style=flat-square&logo=open-source-initiative)

<!-- 第三行：开发语言 -->
![GitHub top language](https://img.shields.io/github/languages/top/your-username/your-repo?style=flat-square&logo=javascript)
```

参数说明：
- `style=flat-square`：方形扁平风格（推荐）
- `logo=`：添加图标（支持 GitHub, Git, JavaScript 等）
- `color=`：自定义颜色

---

# 第三章：JSON 动态徽章（核心重点）

## 3.1 工作原理

JSON 动态徽章从任意 JSON API 获取数据，通过 JSONPath 表达式提取字段值，实时显示。

流程：

```
你的徽章 → Shields.io → 你的API → 返回JSON → Shields.io提取数据 → 生成徽章
```

## 3.2 URL 结构详解

```
https://img.shields.io/badge/dynamic/json?url=<API地址>&query=<JSONPath>&label=<标签>&color=<颜色>
```

四个必填参数：
1. url: JSON 数据源地址（必须 URL 编码）
2. query: JSONPath 查询表达式（提取数据的语法）
3. label: 徽章左侧标签
4. color: 徽章右侧颜色

## 3.3 第一步：找到你的 JSON API

你需要一个返回 JSON 格式的 API。常见选择：

- 公开 API：NPM, PyPI, Docker Hub 等
- 自建 API：用 Cloudflare Workers、Vercel Functions 等免费服务
- GitHub API：获取仓库、用户等信息

示例 API：获取 NPM 包下载量

```
https://api.npmjs.org/downloads/point/last-week/react
```

返回数据：

```json
{
  "downloads": 12345678,
  "package": "react",
  "start": "2024-01-01",
  "end": "2024-01-07"
}
```

## 3.4 第二步：编写 JSONPath 表达式

JSONPath 是用来从 JSON 中提取数据的语法：

表达式	含义	示例	
`$`	根对象	`$`	
`$.字段名`	获取顶级字段	`$.downloads`	
`$['字段名']`	同上，用于特殊字符	`$['total-subs']`	
`$.data.字段`	嵌套对象	`$.data.followers`	
`$[0]`	数组第一个元素	`$[0].name`	

在线测试工具：搜索"JSONPath Online Tester"

## 3.5 第三步：构造完整 URL

以 NPM 下载量为例：

原始数据：

```json
{
  "downloads": 12345678
}
```

操作步骤：
1. API 地址编码：
   
```
   https://api.npmjs.org/downloads/point/last-week/react
   ```

   编码后（在线工具转换）：
   
```
   https%3A%2F%2Fapi.npmjs.org%2Fdownloads%2Fpoint%2Flast-week%2Freact
   ```

2. JSONPath 表达式：
   
```
   $.downloads
   ```

3. 完整 URL：
   
```
   https://img.shields.io/badge/dynamic/json?url=https%3A%2F%2Fapi.npmjs.org%2Fdownloads%2Fpoint%2Flast-week%2Freact&query=%24.downloads&label=周下载&color=brightgreen
   ```

## 3.6 实战案例集

案例 1：显示 Bilibili 粉丝数

API 地址：

```
https://api.bilibili.com/x/relation/stat?vmid=你的UID
```

返回数据：

```json
{
  "code": 0,
  "data": {
    "follower": 1234
  }
}
```

完整徽章 URL：

```
https://img.shields.io/badge/dynamic/json?url=https%3A%2F%2Fapi.bilibili.com%2Fx%2Frelation%2Fstat%3Fvmid%3D你的UID&query=%24.data.follower&label=Bilibili&color=ff69b4&logo=bilibili
```

案例 2：显示天气信息

API 地址（OpenWeatherMap，需要申请免费 API Key）：

```
https://api.openweathermap.org/data/2.5/weather?q=Beijing&appid=你的Key&units=metric
```

返回数据：

```json
{
  "main": {
    "temp": 25.6
  }
}
```

完整徽章 URL：

```
https://img.shields.io/badge/dynamic/json?url=https%3A%2F%2Fapi.openweathermap.org%2Fdata%2F2.5%2Fweather%3Fq%3DBeijing%26appid%3D你的Key%26units%3Dmetric&query=%24.main.temp&label=北京温度&color=orange&logo=cloud
```

案例 3：显示任意网站访问量

使用 Substats 服务：

API 地址：

```
https://api.spencerwoo.com/substats/?source=你的服务&queryKey=你的ID
```

返回数据：

```json
{
  "status": 200,
  "data": {
    "totalSubs": 638
  }
}
```

完整徽章 URL：

```
https://img.shields.io/badge/dynamic/json?url=https%3A%2F%2Fapi.spencerwoo.com%2Fsubstats%2F%3Fsource%3D你的服务%26queryKey%3D你的ID&query=%24.data.totalSubs&label=关注数&color=blue
```

## 3.7 添加图标和样式

在 URL 末尾追加参数：

```markdown
<label>&logo=图标名&logoColor=图标颜色&style=样式
```

示例：

```
https://img.shields.io/badge/dynamic/json?url=...&query=...&label=粉丝&color=ff69b4&logo=twitter&logoColor=white&style=flat-square
```

---

# 第四章：Endpoint 动态徽章（完全自定义）

## 4.1 什么是 Endpoint 徽章

Endpoint 徽章是最强大的动态徽章，你完全控制显示内容。只需要提供一个返回特定格式 JSON 的 URL，Shields.io 就会渲染成徽章。

优势：
- 无需公开 API，数据完全私有
- 可显示多行信息（通过 `\n` 换行）
- 支持 Base64 图标
- 缓存时间可控

## 4.2 JSON 端点格式要求

必须包含的字段：

```json
{
  "schemaVersion": 1,
  "label": "左侧文字",
  "message": "右侧文字",
  "color": "颜色"
}
```

可选字段：

```json
{
  "schemaVersion": 1,
  "label": "左侧文字",
  "message": "右侧文字",
  "color": "绿色",
  "labelColor": "灰色",
  "style": "flat-square",
  "logoSvg": "Base64编码的SVG图标",
  "cacheSeconds": 3600
}
```

## 4.3 创建你的端点

方案 A：使用 GitHub Gist（免费且简单）

步骤 1：创建 GitHub Token
1. 进入 GitHub → Settings → Developer settings → Personal access tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `gist` 权限
4. 复制 Token（只显示一次，务必保存）

步骤 2：创建 Gist
1. 访问 gist.github.com
2. 创建新 Gist，文件名为 `my-badge.json`
3. 内容：

```json
{
  "schemaVersion": 1,
  "label": "我的数据",
  "message": "123",
  "color": "blue"
}
```

4. 保存并获取 Gist ID（URL 中的一串字符）

步骤 3：获取原始 URL
点击 Gist 页面的 "Raw" 按钮，复制 URL：

```
https://gist.githubusercontent.com/你的用户名/GistID/raw/文件名.json
```

步骤 4：构造徽章 URL

```
https://img.shields.io/endpoint?url=你的Gist原始URL
```

方案 B：使用静态网站托管

如果你有自己的网站，直接上传 JSON 文件：

1. 创建 `badge.json` 文件
2. 上传到可访问的目录（如 `https://your-site.com/api/badge.json`）
3. 确保能直接访问到原始 JSON 内容
4. 使用：

```
https://img.shields.io/endpoint?url=https://your-site.com/api/badge.json
```

## 4.4 自动化更新端点数据

案例：自动更新项目统计

创建 `.github/workflows/update-badge.yml`：

```yaml
name: Update Dynamic Badge

on:
  schedule:
    - cron: '0 * * * *'  # 每小时执行
  workflow_dispatch:      # 手动触发

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Generate Stats
        id: stats
        run: |
          # 统计代码行数
          CODE_LINES=$(find . -name "*.js" -o -name "*.py" | xargs wc -l | tail -1 | awk '{print $1}')
          echo "codeLines=$CODE_LINES" >> $GITHUB_OUTPUT
          
          # 统计文件数
          FILE_COUNT=$(find . -type f | wc -l)
          echo "fileCount=$FILE_COUNT" >> $GITHUB_OUTPUT
      
      - name: Update Gist
        uses: actions-js/push@v1
        with:
          github_token: ${{ secrets.YOUR_GIST_TOKEN }}
          repository: your-username/your-gist-id
          directory: .
          message: "Update badge data"
        env:
          CODE_LINES: ${{ steps.stats.outputs.codeLines }}
          FILE_COUNT: ${{ steps.stats.outputs.fileCount }}
```

在 Gist 中创建 `update-badge.js`：

```javascript
// 此脚本在 GitHub Actions 中运行
const fs = require('fs');

const badgeData = {
  schemaVersion: 1,
  label: "项目统计",
  message: `${process.env.CODE_LINES}行代码, ${process.env.FILE_COUNT}个文件`,
  color: "informational"
};

fs.writeFileSync('badge.json', JSON.stringify(badgeData));
```

---

# 第五章：样式与美化深度定制

## 5.1 样式参数大全

在徽章 URL 末尾添加参数：

参数	说明	可选值/示例	
`style`	整体风格	`flat`(默认), `flat-square`, `plastic`, `for-the-badge`, `social`	
`logo`	图标名称	`github`, `twitter`, `python`, `docker` (来自 Simple Icons)	
`logoColor`	图标颜色	`white`, `black`, 或十六进制 `ff69b4`	
`logoSize`	图标大小	`auto` 或具体像素值 `20`	
`labelColor`	左侧背景色	`grey`, `ff69b4`	
`color`	右侧背景色	`blue`, `brightgreen`, 或十六进制	
`cacheSeconds`	缓存时间	`3600` (秒)	

## 5.2 图标使用详解

Simple Icons 提供 2000+ 品牌图标：

1. 查找图标：访问 simpleicons.org
2. 获取名称：图标页面显示 slug（如 `python`, `visualstudiocode`）
3. 使用方式：
   
```
   https://img.shields.io/badge/label-message-blue?logo=python
   ```

颜色匹配建议：

```markdown
<!-- 自动使用品牌色 -->
![Python](https://img.shields.io/badge/Python-3776AB?logo=python&logoColor=white)

<!-- 手动指定颜色 -->
![Docker](https://img.shields.io/badge/Docker-2496ED?logo=docker&logoColor=white)

<!-- 仅图标，无文字 -->
![VS Code](https://img.shields.io/badge/-007ACC?logo=visualstudiocode&logoColor=white)
```

## 5.3 风格对比

```markdown
<!-- flat (默认) -->
![flat](https://img.shields.io/badge/style-flat-informational)

<!-- flat-square (推荐) -->
![flat-square](https://img.shields.io/badge/style-flat--square-informational?style=flat-square)

<!-- plastic -->
![plastic](https://img.shields.io/badge/style-plastic-informational?style=plastic)

<!-- for-the-badge -->
![for-the-badge](https://img.shields.io/badge/style-for--the--badge-informational?style=for-the-badge)

<!-- social -->
![social](https://img.shields.io/badge/style-social-informational?style=social)
```

## 5.4 高级技巧：渐变效果（hack）

虽然 Shields.io 不支持真正的渐变，但可以用颜色代码模拟：

```markdown
![渐变](https://img.shields.io/badge/gradient-effect-ff69b4?logo=gradient&logoColor=white)
```

---

# 第六章：GitHub Actions 自动化实战

## 6.1 场景：自动更新版本号徽章

当你的项目发布新版本时，自动更新徽章。

步骤 1：创建 GitHub Secret
1. 进入项目 Settings → Secrets and variables → Actions
2. 点击 "New repository secret"
3. Name: `GIST_TOKEN`
4. Secret: 你的 GitHub Token（有 gist 权限）

步骤 2：创建更新脚本

在项目根目录创建 `scripts/update-version-badge.sh`：

```bash
#!/bin/bash

# 获取最新 Git tag 作为版本号
VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")

# 创建 badge.json
cat > badge.json <<EOF
{
  "schemaVersion": 1,
  "label": "版本",
  "message": "$VERSION",
  "color": "blue"
}
EOF

echo "Version badge updated: $VERSION"
```

步骤 3：创建 Workflow

`.github/workflows/version-badge.yml`：

```yaml
name: Version Badge Auto Update

on:
  push:
    tags:
      - 'v*'  # 只在推送版本标签时触发

jobs:
  update-badge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        
      - name: Update version badge
        run: bash scripts/update-version-badge.sh
        
      - name: Commit to Gist
        uses: actions-js/push@v1
        with:
          github_token: ${{ secrets.GIST_TOKEN }}
          repository: your-username/your-gist-id
          directory: .
```

## 6.2 场景：监控 API 状态

创建一个徽章显示你的 API 是否正常运行：

```yaml
name: API Health Check Badge

on:
  schedule:
    - cron: '*/30 * * * *'  # 每30分钟检查
  workflow_dispatch:

jobs:
  check-api:
    runs-on: ubuntu-latest
    steps:
      - name: Check API
        id: api-check
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://your-api.com/health)
          if [ "$STATUS" = "200" ]; then
            echo "status=up" >> $GITHUB_OUTPUT
            echo "color=brightgreen" >> $GITHUB_OUTPUT
          else
            echo "status=down" >> $GITHUB_OUTPUT
            echo "color=red" >> $GITHUB_OUTPUT
          fi
      
      - name: Update badge
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          auth: ${{ secrets.GIST_TOKEN }}
          gistID: your-gist-id
          filename: api-status.json
          label: API 状态
          message: ${{ steps.api-check.outputs.status }}
          color: ${{ steps.api-check.outputs.color }}
```

---

# 第七章：图标系统完全指南

## 7.1 Simple Icons 使用

Simple Icons 包含 2000+ 品牌图标。

查找图标：
1. 访问 simpleicons.org
2. 搜索品牌（如 "Twitter", "Python"）
3. 点击图标，复制 slug（通常是品牌名小写，空格变连字符）

使用示例：

```markdown
<!-- Twitter -->
![Twitter](https://img.shields.io/badge/Twitter-1DA1F2?logo=twitter&logoColor=white)

<!-- Python -->
![Python](https://img.shields.io/badge/Python-3776AB?logo=python&logoColor=white)

<!-- Docker -->
![Docker](https://img.shields.io/badge/Docker-2496ED?logo=docker&logoColor=white)
```

## 7.2 自定义图标（Base64）

如果 Simple Icons 没有你需要的图标，可以使用 Base64 编码：

步骤 1：准备图标
- 使用 SVG 格式，大小建议 24x24 像素
- 文件大小 不要超过 20KB，否则 URL 会过长报错

步骤 2：转换为 Base64
使用在线工具（搜索"图片转 Base64"）：
1. 上传 SVG 文件
2. 复制 Base64 字符串（以 `data:image/svg+xml;base64,` 开头）

步骤 3：在徽章中使用

```markdown
![自定义](https://img.shields.io/badge/label-message-blue?logo=data:image/svg+xml;base64,PHN2Zz4...)
```

## 7.3 图标颜色自动匹配

Shields.io 会自动检测图标的主色，你也可以手动覆盖：

```markdown
<!-- 自动颜色 -->
![自动](https://img.shields.io/badge/auto-color-blue?logo=twitter)

<!-- 手动指定白色 -->
![手动](https://img.shields.io/badge/manual-color-blue?logo=twitter&logoColor=white)

<!-- 手动指定黑色 -->
![手动](https://img.shields.io/badge/manual-color-blue?logo=twitter&logoColor=black)
```

---

# 第八章：常见问题排查指南

## 8.1 徽章显示 "invalid response data"

原因：JSONPath 表达式错误或 API 返回格式不符合预期

排查步骤：
1. 在浏览器直接访问 API URL，确认能返回 JSON
2. 使用 JSONPath 测试工具验证表达式
3. 检查 JSONPath 语法：
   - 正确：`$.data.total`
   - 错误：`data.total`（缺少 `$`）
   - 错误：`$data.total`（缺少 `.`）

## 8.2 徽章显示 "resource not found"

原因：URL 编码错误或 API 地址无效

排查步骤：
1. 对 API URL 进行完整编码（包括 `?` 和 `&`）
2. 确认 API 不需要身份验证
3. 检查是否有 CORS 限制（Shields.io 需要跨域支持）

## 8.3 徽章加载缓慢或超时

原因：API 响应慢或 Shields.io 缓存策略

解决方案：
1. 添加缓存参数：
   
```
   ...&cacheSeconds=3600
   ```

   单位是秒，3600=1小时

2. 优化 API：确保 API 在 2 秒内响应
3. 使用 GitHub Actions 缓存：参考第六章，将数据缓存到 Gist

8.4 414 Request-URI Too Large

原因：Base64 图标数据过大或 URL 过长

解决方案：
1. 压缩 SVG 图标（移除不必要的元数据）
2. 减小图标尺寸（20x20 像素足够）
3. 改用 URL 图标（如果可用）
4. 使用 Endpoint 徽章，将数据放在服务器端

8.5 GitHub API 速率限制

症状：徽章偶尔显示 "api rate limit exceeded"

解决方案：
1. 在 URL 中添加 GitHub Token（公开项目）：
   
```
   ...?token=你的GitHubToken
   ```

2. 使用 `cacheSeconds` 减少请求频率
3. 改用 GitHub Actions 缓存数据到 Gist

8.6 徽章样式不一致

最佳实践：
1. 统一风格：一个项目内所有徽章使用相同 `style`
2. 统一高度：`flat-square` 和 `for-the-badge` 高度一致
3. 分组展示：用 HTML 组织布局

```html
<div align="center">
  <img src="badge1" alt="badge1" />
  <img src="badge2" alt="badge2" />
  <br>
  <img src="badge3" alt="badge3" />
</div>
```

---

# 第九章：性能优化与最佳实践

## 9.1 徽章数量控制

- 单页面建议：不超过 8 个徽章
- 分组展示：按功能分组，避免一排过长
- 延迟加载：在网页中使用 `loading="lazy"`

```html
<img src="badge-url" loading="lazy" alt="badge" />
```

## 9.2 缓存策略

场景	推荐缓存时间	参数	
版本号	1 小时	`cacheSeconds=3600`	
下载量	6 小时	`cacheSeconds=21600`	
构建状态	5 分钟	`cacheSeconds=300`	
用户统计	1 天	`cacheSeconds=86400`	

## 9.3 颜色语义化

建立团队颜色规范：

```markdown
<!-- 成功/正常 -->
![成功](https://img.shields.io/badge/status-success-brightgreen)

<!-- 警告/注意 -->
![警告](https://img.shields.io/badge/status-warning-orange)

<!-- 错误/失败 -->
![错误](https://img.shields.io/badge/status-error-red)

<!-- 信息/中性 -->
![信息](https://img.shields.io/badge/status-info-blue)
```

## 9.4 备份策略

对于关键徽章，建议准备静态备用徽章：

```markdown
<!-- 主徽章（动态） -->
![主徽章](https://img.shields.io/badge/dynamic/json?url=...)

<!-- 备用徽章（静态） -->
![备用徽章](https://img.shields.io/badge/status-static%20backup-lightgrey)
```

---

# 第十章：完整项目实战

## 10.1 场景：创建一个项目仪表盘

目标：为项目创建一个包含版本、构建、下载、许可证的完整徽章组

步骤 1：在 README.md 顶部添加

```markdown
<div align="center">

<!-- 版本和构建 -->
[![Version](https://img.shields.io/github/release/your-username/your-repo?style=flat-square&logo=github)](https://github.com/your-username/your-repo/releases)
[![Build](https://img.shields.io/github/workflow/status/your-username/your-repo/CI?style=flat-square&logo=github-actions&logoColor=white)](https://github.com/your-username/your-repo/actions)

<!-- 统计 -->
[![Stars](https://img.shields.io/github/stars/your-username/your-repo?style=flat-square&logo=github&color=gold)](https://github.com/your-username/your-repo/stargazers)
[![Forks](https://img.shields.io/github/forks/your-username/your-repo?style=flat-square&logo=github)](https://github.com/your-username/your-repo/network)
[![Downloads](https://img.shields.io/badge/dynamic/json?url=https%3A%2F%2Fapi.npmjs.org%2Fdownloads%2Fpoint%2Flast-month%2F你的包名&query=%24.downloads&label=月下载&style=flat-square&logo=npm&color=red)](https://www.npmjs.com/package/你的包名)

<!-- 质量和许可证 -->
[![License](https://img.shields.io/github/license/your-username/your-repo?style=flat-square&logo=open-source-initiative)](LICENSE)
[![Coverage](https://img.shields.io/codecov/c/github/your-username/your-repo?style=flat-square&logo=codecov&logoColor=white)](https://codecov.io/gh/your-username/your-repo)

</div>
```

步骤 2：创建徽章说明文档

在项目根目录创建 `BADGES.md`：

```markdown
# 徽章说明

本项目使用 Shields.io 动态徽章展示实时状态。

## 徽章列表

| 徽章 | 来源 | 更新频率 |
|------|------|----------|
| 版本 | GitHub Releases | 立即 |
| 构建 | GitHub Actions | 每次提交 |
| Stars | GitHub API | 实时 |
| 下载量 | NPM API | 每天 |
| 覆盖率 | Codecov | 每次测试 |

## 维护

如果需要修改徽章配置，编辑 `README.md` 中的 URL 参数。
```

## 10.2 场景：个人主页技能展示

在 GitHub Profile README 中展示技术栈：

```markdown
## 🛠️ 技术栈

### 前端
![HTML5](https://img.shields.io/badge/HTML5-E34F26?style=flat-square&logo=html5&logoColor=white)
![CSS3](https://img.shields.io/badge/CSS3-1572B6?style=flat-square&logo=css3&logoColor=white)
![JavaScript](https://img.shields.io/badge/JavaScript-F7DF1E?style=flat-square&logo=javascript&logoColor=black)
![React](https://img.shields.io/badge/React-61DAFB?style=flat-square&logo=react&logoColor=black)
![Vue](https://img.shields.io/badge/Vue.js-4FC08D?style=flat-square&logo=vuedotjs&logoColor=white)

### 后端
![Node.js](https://img.shields.io/badge/Node.js-339933?style=flat-square&logo=nodedotjs&logoColor=white)
![Python](https://img.shields.io/badge/Python-3776AB?style=flat-square&logo=python&logoColor=white)
![Docker](https://img.shields.io/badge/Docker-2496ED?style=flat-square&logo=docker&logoColor=white)

### 工具
![Git](https://img.shields.io/badge/Git-F05032?style=flat-square&logo=git&logoColor=white)
![VS Code](https://img.shields.io/badge/VS%20Code-007ACC?style=flat-square&logo=visualstudiocode&logoColor=white)
```

---

# 总结与下一步行动

## 学习路径回顾

1. 入门（第1-2章）：掌握静态徽章和 GitHub 内置动态徽章
2. 进阶（第3-4章）：学会 JSON 和 Endpoint 动态徽章，实现完全自定义
3. 精通（第5-7章）：美化徽章，添加图标，使用 GitHub Actions 自动化
4. 实战（第8-10章）：排查问题，优化性能，应用到实际项目

立即开始的 3 个步骤

步骤 1：为你的项目添加基础徽章（10分钟）

```markdown
复制第二章的 GitHub 徽章代码，替换用户名和仓库名
```

步骤 2：创建一个动态徽章（30分钟）

```markdown
1. 找到一个公开 JSON API
2. 使用在线 JSONPath 测试工具验证表达式
3. 构造完整 URL 并测试
```

步骤 3：设置自动化（1小时）

```markdown
1. 创建 GitHub Gist 和 Token
2. 复制示例 Workflow 文件
3. 配置定时任务或触发条件
```

## 进阶探索

- 贡献到 Shields.io：为喜欢的服务添加官方支持
- 自建徽章服务：使用 shields.io 开源代码搭建私有实例
- 创意设计：结合 CSS 动画创建动态效果
- 监控告警：集成到监控系统，实时显示服务状态

## 资源汇总

- 官方文档：https://shields.io/
- Simple Icons：https://simpleicons.org/
- JSONPath 测试：搜索 "JSONPath Online Evaluator"
- URL 编码：搜索 "URL Encode Online"
- Base64 转换：搜索 "Image to Base64"

现在，你已经掌握了 Shields.io 动态徽章的完整技能栈。从简单的静态标签到复杂的自动化数据展示，你可以为任何项目打造专业、实时的视觉状态板

现在开始动手吧！